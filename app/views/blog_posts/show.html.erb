<p id="notice"><%= notice %></p>

<br><br>
<div class="container">
  <div class="post">
    <h2 class="post--title"><%= @blog_post.title %></h2>
    <p><%= @blog_post.created_at.strftime('%A, %b %d') %></p>
    <% if @blog_post.photo.present? %>
      <%= image_tag @blog_post.photo.url %>
    <% end %>
    <p><%= @blog_post.content.html_safe %></p>
    <p><%= link_to 'Back', blog_posts_path, class: 'button'%></p>
    <% if @blog_post.belongs_to_current_user?(current_user) %>
      <p><%= link_to 'Edit', edit_blog_post_path(@blog_post), class: 'button' %></p>
      <p><%= link_to 'Destroy', @blog_post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'button-warning' %></p>
    <% end %>

  </div>

</div>



<script>
  jQuery(document).ready(function($){
    Rainbow.color();
  });


</script>
